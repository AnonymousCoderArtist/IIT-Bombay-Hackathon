# Smart Campus AI service — uv managed
# uv.lock commit rakho taaki deps reproducible rahein.
# Naya machine: `uv sync` karke chal jayega (pyproject.toml dekh ke).

.venv/
.env
data/
__pycache__/
*.pyc

# Models repo me commit hain (services/ai/models/) — unhe ignore mat karo
# ~/.uniface/models wali auto-download only tab hoti hai jab repo models nahi hain
